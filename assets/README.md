# Assets

Drop these two files in this folder to complete the site:

- `portrait.jpg` — your photo for the homepage. Displayed in grayscale via CSS.
- `Nicole-Ward-Resume.pdf` — the PDF linked from the "DOWNLOAD PDF" button on the Resume page.

Until `portrait.jpg` is added, the homepage shows a placeholder box instead of a broken image.
